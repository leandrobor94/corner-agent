name: Corner Agent

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Run corner analysis
        run: node index.js ci
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      - name: Commit updated data
        run: |
          git config user.name "Corner Agent"
          git config user.email "agent@corner.local"
          for f in predictions.json leagues.json weights.json; do [ -f "$f" ] && git add "$f"; done
          git diff --quiet && git diff --staged --quiet || git commit -m "auto: update corner data [skip ci]"
          git push
